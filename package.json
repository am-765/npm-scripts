{
  "name": "ver1.0",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist/",
    "build:pug": "pug src/*.pug -P -o dist/",
    "build:css": "mkdirp dist/assets/css && stylus ./src/assets/css/common.styl -c -o ./dist/assets/css/common.css -m --sourcemap-base ../assets",
    "//1": "「_build:js」は圧縮難読化用、仕様の際は_を外して通常の「build:js」に_をつける",
    "_build:js": "cpx \"src/**/*.js\" dist/ && browserify -t babelify src/**/js/*.js -o dist/**/js/*.js",
    "build:js": "cpx \"src/**/*.js\" dist/ && browserify -t babelify src/assets/js/common.js | uglifyjs -c -m > dist/assets/js/common.js",
    "build:images": "imagemin src/**/imgs/* -o dist/assets/imgs/",
    "build": "yarn run clean & npm-run-all -p build:*",
    "watch:pug": "pug -w src/*.pug -P -o dist/",
    "watch:css": "stylus -w ./src/assets/css/common.styl -c -o ./dist/assets/css/common.css -m --sourcemap-base ../assets",
    "//2": "「_watch:js」は圧縮難読化用、仕様の際は_を外して通常の「watch:js」に_をつける",
    "_watch:js": "watchify -t babelify src/**/js/*.js -o dist/**/js/*.js -v",
    "watch:js": "watchify -t babelify src/**/js/*.js -o 'uglifyjs -c -m > dist/assets/js/common.js' -v",
    "watch:images": "onchange 'src/**/*.{png,jpg,gif,svg}' -e '**/*.DS_Store' -- yarn run build",
    "watch:server": "browser-sync start -s dist/ -f 'dist, **/*.html, !node_modules/**/*'",
    "watch": "npm-run-all -p watch:*"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "8",
    "browser-sync": "^2.26.3",
    "browserify": "^16.2.3",
    "cpx": "^1.5.0",
    "imagemin-cli": "^3.0.0",
    "mkdirp": "^0.5.1",
    "npm-run-all": "^4.1.3",
    "onchange": "^5.0.2",
    "pug-cli": "^1.0.0-alpha6",
    "rimraf": "^2.6.2",
    "stylus": "^0.54.5",
    "uglify-es": "^3.3.9",
    "watchify": "^3.11.0"
  }
}
